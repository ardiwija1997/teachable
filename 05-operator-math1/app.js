const num1 = 100;
const numb2 = 50;
let val;

// val = Math.pow(10,5); // pangkat
// val = Math.min(5,2,12) // mencari nilai terendah sebaliknya max
// val = Math.random(); // hasil random (acak)
// val = Math.random() *50; // membatasi nilai yang ingin diacak
//  val = Math.floor(Math.random() *50); // membatasi nilai & membulatkan nilai

console.log(val);