const num1 = 100;
const numb2 = 50;
let val;

// simple math operator 
val = num1 + num2; 
// val = num1 % num2; //modulus hasil bagi

// Math Object
// val = Math.PI; // nilai p1
// val = Math.E; // nilai constant
// val = Math.round(3.7); // nilai bulat
// val = Math.ceil(2.4); // dibulatkan keatas
// val = Math.floor(4.7); // dibulatkan kebawah
// val = Math.sqrt(64); // mencari nilai akar kuadrat
// val = Math.abs(-5); // mengubah menjadi nilai positif



console.log(val);